// src/app/builder/layout.tsx
// Layout for builder pages - full screen without header/footer
export default function BuilderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

